# Configuracion base de Nginx para publicar TaskFlow.
server {
  listen 80;
  server_name taskflow.local;

  root /var/www/taskflow;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  location /api/ {
    proxy_pass http://localhost:3000/api/;
  }
}
